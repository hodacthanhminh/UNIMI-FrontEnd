import React from "react";

const StoreTag = ({ tagName }) => (
  <div className="store-deatail__tag-wrapper">
    <span className="store-detail__tag-name">{tagName} </span>
  </div>
);

export default StoreTag;
